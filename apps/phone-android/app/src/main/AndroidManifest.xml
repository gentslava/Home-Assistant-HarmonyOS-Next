<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- HA REST API over the local network / Nabu Casa -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Wear Engine -->
    <uses-permission android:name="com.huawei.wearengine.permission.DEVICE_MANAGER" />
    <uses-permission android:name="com.huawei.wearengine.permission.NOTIFY" />

    <!-- Android 11+ package visibility: the Wear Engine SDK binds to the Huawei Health /
         HMS Core services via an implicit intent. Without these <queries>, queryIntentServices
         returns empty ("implicitIntent List are null") and Wear Engine never reaches the
         permission dialog. -->
    <queries>
        <package android:name="com.huawei.health" />
        <package android:name="com.huawei.hwid" />
    </queries>

    <application
        android:allowBackup="true"
        android:label="HA Companion"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <!-- HMS Core / Wear Engine app identity (AGC app_id for ru.gentslava.homeassistant.companion).
             Wear Engine authorizes the app via HMS Core using this app_id; without it requestPermission
             fails with WearEngineException code 12 (Internal error). Not a secret. -->
        <meta-data
            android:name="com.huawei.hms.client.appid"
            android:value="appid=118028393" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
